---

dependencies:
  - role: sansible.java
    version: v1.0
    java:
      version: "{{ elasticsearch.java.version }}"
    tags:
      - build

galaxy_info:
  author: "David Reed"
  description: "Install Elasticsearch."
  license: MIT
  min_ansible_version: 1.9
  platforms:
    - name: Ubuntu
      versions:
        - all
  categories:
    - development
